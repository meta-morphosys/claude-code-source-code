/**
 * Public `Settings` type for the Agent SDK re-export.
 * Aligns with the CLI settings JSON schema (`SettingsJson`).
 */
export type { SettingsJson as Settings } from '../../utils/settings/types.js'
